 <% @user ||= current_user %>

<div class="stats">

    <a href="<%= following_user_path(@user) %>">
        <span>following</span>
        <strong id="following" class="stat"><%= @user.followings.count %></strong>
    </a>

<div>
